import{load}from'./common.js';import{mountShell,badge,formatCurrency}from'./ui.js';mountShell();
const files=['vehicle-listings.json','camper-listings.json','camperization-quotes.json','platforms.json','sources.json'];const all=[];for(const file of files){const d=await load('data/'+file),rows=d.sample||d.quotes||d.platforms||d.sources||[];rows.forEach(r=>all.push({...r,file,date:d.asOf||'N/D',status:d.status||d.note||''}))}
document.querySelector('#source-stats').innerHTML=`<div class="kpi"><div class="kpi-label">Registros</div><div class="kpi-value">${all.length}</div><div class="kpi-context">Trazables en JSON</div></div><div class="kpi warning"><div class="kpi-label">Pendientes de revalidar</div><div class="kpi-value">${all.length}</div><div class="kpi-context">Acceso externo limitado</div></div><div class="kpi"><div class="kpi-label">Datasets</div><div class="kpi-value">${files.length}</div><div class="kpi-context">Separados del modelo</div></div>`;
document.querySelector('#sources-table').innerHTML=`<div class="table-wrap tall"><table><thead><tr><th>Categoría</th><th>Referencia</th><th>Fecha</th><th>Valor</th><th>Estado</th><th>Confianza</th><th>URL</th></tr></thead><tbody>${all.map(x=>`<tr><td>${x.file}</td><td>${x.model||x.supplier||x.name||x.variable||'Referencia'}</td><td>${x.date}</td><td>${x.cash?formatCurrency(x.cash):x.net?formatCurrency(x.net):x.commission||'N/D'}</td><td>${badge('Pendiente revalidación','warning')}</td><td>Baja / orientativa</td><td><a href="${x.url}" rel="noopener">Abrir fuente</a></td></tr>`).join('')}</tbody></table></div>`;

