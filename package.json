{
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8000",
    "test:browser": "node --test e2e/v4-browser.test.mjs",
    "test:all": "npm test && npm run test:browser"
  },
  "type": "module",
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
